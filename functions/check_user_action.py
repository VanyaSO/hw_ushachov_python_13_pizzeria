def check_user_action():
    # Ask the user about the next action until he chooses one of the suggested ones
    while True:
        next_action = input("Select what you want --- (check basket: 1), (get a check: 2), (order: press'Enter'): ")
                    
        return(next_action)      
            
            